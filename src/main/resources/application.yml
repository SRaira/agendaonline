security:
  jwt:
    token:
      secret-key: 54cr37
      expire-length: 3600000
spring:
  flyway:
    baselineOnMigrate: true
  datasource:
      url: jdbc:mysql://localhost:3306/agenda?useTimezone=true&serverTimezone=UTC
      username: root
      password: Hellencbp@#1911#@
      driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
springdoc:
  pathsToMatch:
    - /auth/**
  swagger-ui:
    use-root-path: true


      #defer-datasource-initialization: true
  #sql:
    #init:
      #mode: always


            
      
           
      